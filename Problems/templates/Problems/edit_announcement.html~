{% extends 'Problems/base.html' %}

{% block title %}
<title>Edit Item-Mat237!</title>
{% endblock %}

{% block content %}
    <form method="POST" >
    {% csrf_token %}
        <table class="form-table">
            {% for field in form %}
            <tr>
                {{field.errors}}
                <td> {{ field.label_tag }} </td>
                <td> {{ field }} <br>
                {% if field.help_text %}
                <p> {{field.help_text|safe }} </p>
                {% endif %}
                </td>
            </tr>
            {% endfor %}
        </table>

        <input type="submit" value="Submit" />
    </form>
{% endblock %}
